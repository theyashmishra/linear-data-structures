#include<bits/stdc++.h>
using namespace std;

int main() {
	set<int> s;
	s.insert(5);
	s.insert(3);
		s.insert(8);
			s.insert(9);
			for(int x:s){
			    cout<<x<<" ";
			}
			auto it=s.find(100);
			(it==s.end())?(cout<<"not found"<<endl):cout<<"found"<<endl;
	auto it1=s.upper_bound(10);
	auto it2=s.lower_bound(3);
	cout<<*it1<<" "<<*it2;
	return 0;
}
